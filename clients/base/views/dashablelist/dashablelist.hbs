{{!--
/*
 * Your installation or use of this SugarCRM file is subject to the applicable
 * terms available at
 * http://support.sugarcrm.com/Resources/Master_Subscription_Agreements/.
 * If you do not agree to all of the applicable terms or do not have the
 * authority to bind the entity as an authorized representative, then do not
 * install or use this SugarCRM file.
 *
 * Copyright (C) SugarCRM Inc. All rights reserved.
 */
--}}
{{#if collection.dataFetched}}
    {{#each meta.panels}}
        <table class="table table-striped dataTable">
            <thead>
                <tr>
                    {{#each fields}}
                        <th class="{{#isSortable ../../module this}}sorting sorting_{{#eq ../../../collection.orderBy.field name}}{{../../../../orderBy.direction}}{{/eq}} orderBy{{name}}{{/isSortable}}{{#if widthClass}} {{widthClass}}{{/if}}"
                        {{#if styles}} style="{{styles}}"{{/if}}
                        {{#if name}} data-fieldname="{{name}}"{{/if}} data-orderby="{{orderBy}}">
                            <span>{{str label ../../module}}</span>
                        </th>
                    {{/each}}
                </tr>
            </thead>
            <tbody>
                {{#each ../collection.models}}
                    <tr class="single">
                        {{#each ../fields}}
                            <td{{#if align}} class="{{align}}"{{/if}}>{{field ../../../this model=../this template=../../../viewName}}</td>
                        {{/each}}
                    </tr>
                {{/each}}
            </tbody>
        </table>
    {{/each}}
    {{#unless collection.length}}
        <div class="block-footer">
            {{str "LBL_NO_DATA_AVAILABLE"}}
        </div>
    {{/unless}}
{{/if}}
